{
  "$schema": "https://json.schemastore.org/package.json",
  "type": "module",
  "license": "MIT",
  "private": true,
  "sideEffects": false,
  "name": "@motiro/monorepo",
  "description": "Explore a versatile assortment of helper utility functions",
  "packageManager": "bun@1.4.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/meluiz/motiro"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "clean": "bun run --filter '*' clean",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "check": "biome check .",
    "check:fix": "biome check . --write",
    "lint": "biome lint .",
    "lint:fix": "biome lint . --write",
    "format": "biome format .",
    "format:fix": "biome format . --write",
    "typecheck": "tsc --noEmit",
    "preflight": "bun run lint && bun run typecheck",
    "prepare": "lefthook install",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "workspaces": {
    "packages": ["packages/*"],
    "catalog": {
      "@arethetypeswrong/cli": "^0.18.5",
      "@biomejs/biome": "^2.5.11",
      "publint": "^0.3.24",
      "tsdown": "^0.22.14",
      "typescript": "^7.0.2"
    }
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@biomejs/biome": "catalog:",
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@vitest/ui": "^4.1.11",
    "lefthook": "^2.1.12",
    "publint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.11"
  },
  "resolutions": {
    "@motiro/core": "workspace:*",
    "@motiro/types": "workspace:*",
    "@motiro/guard": "workspace:*",
    "@motiro/string": "workspace:*",
    "@motiro/number": "workspace:*",
    "@motiro/collection": "workspace:*"
  }
}
